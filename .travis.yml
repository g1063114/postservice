language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# Travis CI Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# CI 실행 완료 시 메일 알람
notifications:
  email:
    recipients:
      - g1063114@gmail.com